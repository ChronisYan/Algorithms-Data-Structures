#include <iostream>

int main()
{
    // Calculate sum of N first natural number:
    // n*(n+1) / 2
    int n = 8;
    printf("Sum of %d first numbers: %d\n", n, n * (n + 1) / 2);

    // Calculate sum of a Arithmetic progression, where a = first number, d = difference, n = number of numbers
    // (n/2) * (2*a + d*(n-1))

    // Calculate sum of a Geometric progression, where a = first number, r = common ratio, n = number of numbers
    //a*(1-r^n) / 1 -r
}