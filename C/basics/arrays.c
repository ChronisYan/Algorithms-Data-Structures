#include <stdio.h>

int main()
{
    // Declare an array of size 5
    int A[5];

    // Declare and Initiate an array of size n
    int B[4] = {3, 6, 8, 10};

    // Traverse an array
    for (int i = 0; i < 4; i++)
    {
        printf("%d ", B[i]);
    }
    printf("\n");

    return 0;
}